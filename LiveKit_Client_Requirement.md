# LiveKit 客户端需求文档

## 1. 项目概述

### 1.1 项目名称

LiveKit - 一款类抖音风格的桌面直播应用

### 1.2 项目定位

将推流端和播放端合二为一的桌面直播应用，用户既是主播也是观众。类似抖音桌面版，登录后可浏览直播大厅、观看直播、自己开播。

### 1.3 目标用户

C++ 方向大学生项目，需展示 C++ 核心能力（多线程、网络编程、音视频处理、设计模式），最终上架 GitHub 并写入简历。

### 1.4 运行环境

- 操作系统：Windows 11
- 网络环境：局域网（连接 Ubuntu 虚拟机上的服务端）
- 依赖服务：Ubuntu 虚拟机上运行的 LiveKit Server + Nginx-RTMP

***

## 2. 技术栈

| 维度      | 技术选型                    | 说明                                             |
| ------- | ----------------------- | ---------------------------------------------- |
| 语言      | C++11/14                | 使用现代 C++ 特性                                    |
| GUI 框架  | Qt 5                    | QWidget 体系                                     |
| 构建系统    | CMake                   | 跨平台构建                                          |
| 视频编解码   | FFmpeg 4.2.2            | 使用新版 API（avcodec\_send\_packet/receive\_frame） |
| 音频播放    | SDL2                    | 音频输出和回调                                        |
| 视频渲染    | QOpenGLWidget           | OpenGL 纹理渲染                                    |
| 摄像头采集   | OpenCV 4.2.0            | VideoCapture                                   |
| 桌面采集    | QScreen::grabWindow     | Qt 原生屏幕截图                                      |
| 音频采集    | QAudioInput             | Qt Multimedia                                  |
| 业务通信    | HTTP + WebSocket        | 与服务端交互                                         |
| 流媒体     | RTMP                    | 推流到 Nginx-RTMP                                 |
| JSON 解析 | nlohmann/json 或 Qt JSON | HTTP 响应解析                                      |

***

## 3. 功能需求

### 3.1 用户系统

#### 3.1.1 注册

- 用户名（3-20字符，字母数字下划线）
- 密码（6-20字符，需确认）
- 选择预设头像（8-12个内置头像）
- 注册成功后自动登录

#### 3.1.2 登录

- 用户名 + 密码登录
- 登录成功后服务端返回 token，客户端本地保存
- 下次启动自动登录（token 有效期内）
- 登录失败提示错误信息

#### 3.1.3 个人信息

- 显示用户名、头像、ID
- 显示直播历史记录数量

### 3.2 直播大厅

#### 3.2.1 大厅布局

- 顶部：应用 Logo + 搜索栏 + 用户头像（点击进入个人信息）
- 主体：网格卡片布局展示直播间列表（每行3-4个卡片）
- 每个卡片显示：直播间封面截图、主播头像、主播名称、直播标题、观众人数、直播分类标签
- 底部导航栏：直播大厅 / 我要开播 / 个人中心

#### 3.2.2 直播间列表

- 从服务端 HTTP API 获取正在直播的房间列表
- 支持按分类筛选（全部/游戏/聊天/音乐/其他）
- 列表自动刷新（每30秒或手动下拉刷新）
- 点击卡片进入直播间观看

#### 3.2.3 回放列表

- 已结束的直播显示"回放"标签
- 点击回放卡片进入回放播放器

### 3.3 直播观看（观众端）

#### 3.3.1 视频播放

- RTMP 拉流播放（FFmpeg 解码 + SDL2 音频 + QOpenGLWidget 渲染）
- 音视频同步（以音频时钟为基准）
- 播放控制：音量调节、全屏切换
- 缓冲状态提示（加载中动画）

#### 3.3.2 弹幕系统

- 固定位置弹幕（类似抖音，消息从下往上弹出）
- 弹幕输入框在底部
- 发送弹幕通过 WebSocket 实时传输
- 弹幕显示区域在视频右侧或叠加在视频上
- 弹幕样式：半透明背景 + 白色文字，自动滚动
- 最多显示最近50条弹幕

#### 3.3.3 礼物系统

- 礼物面板（点击礼物图标展开）
- 预设6-8种免费礼物（小花、鼓掌、比心、火箭、皇冠、烟花等）
- 每种礼物有图标和名称
- 点击礼物发送，通过 WebSocket 通知
- 收到礼物时播放动画效果（QPropertyAnimation）
- 礼物动画显示在视频区域上方

#### 3.3.4 点赞系统

- 点赞按钮（红心图标）
- 点击后屏幕飘出红心动画（QPropertyAnimation，从下往上飘出并渐隐）
- 连续点赞支持连击效果
- 点赞数实时显示

#### 3.3.5 主播信息

- 左上角显示主播头像、名称
- 在线观众人数
- 观众列表（可展开查看在线观众）

#### 3.3.6 退出直播间

- 返回按钮回到大厅
- 退出时断开 WebSocket 连接和 RTMP 流

### 3.4 直播推流（主播端）

#### 3.4.1 开播设置

- 直播标题（必填，2-30字符）
- 直播分类选择（游戏/聊天/音乐/其他）
- 直播模式选择：
  - 摄像头直播：仅摄像头画面
  - 桌面直播：仅桌面画面
  - 桌面+画中画：桌面画面 + 右下角摄像头小窗

#### 3.4.2 推流功能

- 视频编码：H.264（superfast/zerolatency 预设）
- 音频编码：AAC（FLTP, 44.1kHz）
- 封装格式：FLV
- 推流协议：RTMP → Nginx-RTMP
- 推流地址：rtmp\://{server\_ip}/live/{stream\_key}
- 帧率：25fps
- 画中画：摄像头画面缩放后叠加到桌面画面右下角（320x240）

#### 3.4.3 直播控制

- 开始直播 / 停止直播按钮
- 直播中显示实时预览
- 在线观众人数显示
- 弹幕接收和显示（同观众端）
- 礼物和点赞接收和显示

#### 3.4.4 画中画窗口

- 桌面直播模式下，画中画窗口可拖拽移动
- 画中画窗口可关闭/重新打开
- 画中画窗口始终置顶

### 3.5 直播回放

#### 3.5.1 回放播放器

- 复用视频播放模块（FFmpeg 解码 + SDL2 + QOpenGLWidget）
- 支持进度条拖拽
- 支持暂停/继续
- 显示总时长和当前时间

#### 3.5.2 回放列表

- 在直播大厅中显示已结束的直播回放
- 回放卡片显示：封面截图、主播名称、直播标题、时长

### 3.6 主题系统

#### 3.6.1 深色主题

- 深色背景 (#1a1a2e, #16213e)
- 亮色文字 (#e0e0e0)
- 强调色 (#0f3460, #e94560)

#### 3.6.2 浅色主题

- 浅色背景 (#ffffff, #f5f5f5)
- 深色文字 (#333333)
- 强调色 (#4a90d9, #e94560)

#### 3.6.3 主题切换

- 设置页面提供主题切换开关
- 切换后全局立即生效
- 主题偏好保存到本地配置

### 3.7 设置页面

- 服务器地址配置（默认为局域网虚拟机 IP）
- 推流画质选择（高清/标准/流畅）
- 主题切换（深色/浅色）
- 关于页面（项目信息、版本号）

***

## 4. 非功能需求

### 4.1 性能要求

- 视频播放延迟 < 3秒（局域网 RTMP）
- 推流帧率稳定 25fps
- 弹幕消息延迟 < 500ms
- 内存占用 < 500MB

### 4.2 稳定性要求

- 网络断开自动重连
- 推流失败自动重试（最多3次）
- 异常退出不丢失用户登录状态

### 4.3 代码质量

- 使用现代 C++ 特性（智能指针、lambda、auto、范围 for 等）
- 合理使用设计模式（观察者、工厂、单例等）
- 清晰的模块划分和接口设计
- CMake 管理第三方依赖

***

## 5. 客户端与服务端接口约定

### 5.1 HTTP API

| 接口      | 方法   | 路径                        | 说明                                                                                                       |
| ------- | ---- | ------------------------- | -------------------------------------------------------------------------------------------------------- |
| 注册      | POST | /api/register             | 请求：{username, password} 响应：{code, msg, token}                                                            |
| 登录      | POST | /api/login                | 请求：{username, password} 响应：{code, msg, token, user\_info}                                                |
| 获取直播间列表 | GET  | /api/live/rooms           | 响应：{code, rooms:\[{room\_id, title, anchor\_name, anchor\_avatar, viewer\_count, category, cover\_url}]} |
| 获取回放列表  | GET  | /api/live/replays         | 响应：{code, replays:\[{replay\_id, title, anchor\_name, duration, cover\_url, play\_url}]}                 |
| 创建直播间   | POST | /api/live/create          | 请求：{token, title, category, mode} 响应：{code, room\_id, stream\_key, push\_url}                            |
| 结束直播    | POST | /api/live/end             | 请求：{token, room\_id} 响应：{code, msg}                                                                      |
| 获取直播间信息 | GET  | /api/live/room/{room\_id} | 响应：{code, room\_info, viewer\_list}                                                                      |
| 获取头像列表  | GET  | /api/avatars              | 响应：{code, avatars:\[{id, url}]}                                                                          |

### 5.2 WebSocket 消息格式

连接地址：ws\://{server\_ip}:8080/ws?token={token}\&room\_id={room\_id}

#### 客户端发送消息

```json
{
  "type": "danmaku",
  "content": "你好"
}
```

```json
{
  "type": "gift",
  "gift_id": 1
}
```

```json
{
  "type": "like"
}
```

#### 服务端推送消息

```json
{
  "type": "danmaku",
  "username": "user1",
  "content": "你好",
  "timestamp": 1716192000
}
```

```json
{
  "type": "gift",
  "username": "user1",
  "gift_id": 1,
  "gift_name": "小花",
  "timestamp": 1716192000
}
```

```json
{
  "type": "like",
  "count": 128,
  "timestamp": 1716192000
}
```

```json
{
  "type": "viewer_count",
  "count": 50
}
```

```json
{
  "type": "viewer_join",
  "username": "user2"
}
```

```json
{
  "type": "viewer_leave",
  "username": "user3"
}
```

***

## 6. UI 页面清单

| 页面      | 说明                         |
| ------- | -------------------------- |
| 登录页     | 用户名密码输入 + 登录/注册切换          |
| 注册页     | 用户名密码确认 + 头像选择             |
| 直播大厅    | 网格卡片布局 + 分类筛选 + 底部导航       |
| 直播间（观众） | 视频播放 + 弹幕 + 礼物 + 点赞 + 主播信息 |
| 直播间（主播） | 预览画面 + 弹幕 + 礼物 + 直播控制      |
| 开播设置页   | 标题/分类/模式选择                 |
| 回放播放页   | 视频播放 + 进度条 + 暂停/继续         |
| 个人中心    | 头像/用户名/直播历史                |
| 设置页     | 服务器地址/画质/主题/关于             |

